// NB!! Install base dependencies first

// Import all dependencies :
// const express = require('express');

// const cors = require('cors')

// Create a port for your webserver
// const PORT = 3001 || process.env.PORT

// Instantiate the web server :
// const app = express();

// Make the server listen on your port
// app.listen(PORT,()=>{

//	console.log('Server is up and running')

//   })
let fakeDB = [
  {
    author: "Ryan",
    title: "Do the Dishes",
    description: "Wash the dishes before making dinner tonight",
    completed: false,
  },
];
// Create a get route to test :  open your browser and go to http://localhost:3001/

// app.get('/',(req,res)=>{

//	res.send('Welcome to the server')

//   })
